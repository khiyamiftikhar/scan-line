version: "0.1.4"
description: "Component for identifying multiple PWM signals on GPIO, using the MCPWM capture peripheral"
url: "https://github.com/embedblocks/pulse-decoder"
license: "MIT"

dependencies:
  idf: ">=5.0"

files:
   use_gitignore: true
   exclude:
      - ".git/**/*"
      - "docs/**/*"
      - "dist/**/*"
      - "*.docx"
      - "*.txt"