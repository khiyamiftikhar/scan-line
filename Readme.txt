It is the implementation of the Scanner OO design. Although the interfaces are defined, 
but they seem of no use as the scanner is tightly coupled with the capture objects 
because it has to provide the shared timer which is pcpwm timer and hence tighly coupled 
with the implementation

